{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Qt C++",
            "type": "shell",
            "command": "cmake -B build && cmake --build build",
            "group": {
                "kind": "build",
                "isDefault": false
            }
        }
    ]
}